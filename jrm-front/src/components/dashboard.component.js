import React, {useState} from "react";
import PropTypes from "prop-types";

export default function Dashboard(){
    let dashboard
    
    dashboard = <div>
        <h1>JRM-Project Dashboard</h1>
        <p>There will be content for you here I swear</p>
    </div>

    return(
        dashboard
    );
}